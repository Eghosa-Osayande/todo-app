import 'package:flutter/material.dart';

class TodoDetailPage extends StatelessWidget {
  static const name = '/todoDetail';


  const TodoDetailPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}